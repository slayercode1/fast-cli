import { configureRoutes, Controller } from './controller';
import { Get, Patch, Post, Delete, Put, BodyCorps } from './decorator';

export { configureRoutes, Controller, Get, Patch, Post, Delete, Put, BodyCorps };
