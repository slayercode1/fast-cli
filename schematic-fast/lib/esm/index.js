import * as path from 'path';
const templateController = path.join(__dirname, 'controller');
const templateEntity = path.join(__dirname, 'entity');
const templateEnums = path.join(__dirname, 'enum');
const templateInterface = path.join(__dirname, 'interface');
const templateResource = path.join(__dirname, 'resource');
export { templateController, templateEntity, templateEnums, templateInterface, templateResource };
